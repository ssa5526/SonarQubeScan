# SonarQubeScan
A Jenkinsfile that can be used to run a sonarQube scan on a file of many different languages

This requires a project to be setup in both Jenkins and SonarQube as well as a repository in either github or bitbucket. The link to the scan results are located in the output of the jenkinsfile
